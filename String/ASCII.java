package String;
import java.util.*;
public class ASCII {
    public static void main(String[] args) {
        char character = 'F'; 
        int asciiValue = (int) character; // Cast char to int to get the ASCII value
    
        System.out.println("The ASCII value of '" + character + "' is: " + asciiValue);
    }
}
